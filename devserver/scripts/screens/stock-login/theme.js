OnTheFly.registerStyles({
    // Layout
    loginContainer: { padding: 0, spacing: 0 },
    loginForm: { padding: 24, spacing: 14 },

    // Spacers
    headerGap: { height: 20 },
    sectionGap: { height: 18 },
    smallGap: { height: 6 },
    tinyGap: { height: 4 }
});
